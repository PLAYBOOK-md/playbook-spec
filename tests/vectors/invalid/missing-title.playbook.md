## INPUTS

- `query` (string): Search query

## STEP 1: Search

Search for {{query}} and return results.

This document has no level-1 heading, which is a fatal error per the spec. The INPUTS and STEP sections use level-2 headings but there is no title.
